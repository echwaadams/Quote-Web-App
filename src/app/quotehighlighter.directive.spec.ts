import { QuotehighlighterDirective } from './quotehighlighter.directive';

describe('QuotehighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new QuotehighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
